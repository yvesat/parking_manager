import 'package:flutter/material.dart';

class ParkingSlotRegScreen extends StatelessWidget {
  const ParkingSlotRegScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        centerTitle: true,
        title: const Text('Cadastro de Vagas'),
      ),
    );
  }
}
