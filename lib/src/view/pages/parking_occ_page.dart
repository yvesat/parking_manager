import 'package:flutter/material.dart';

class ParkingOccPage extends StatelessWidget {
  const ParkingOccPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        centerTitle: true,
        title: const Text('Controle de Vagas'),
      ),
    );
  }
}
